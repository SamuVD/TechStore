// using TechStoreFullRestAPI.Models;

// namespace TechStoreFullRestAPI.Repositories;

// public interface ICategoryRepository
// {
//     // Obtiene todas las categorías de la base de datos.
//     IEnumerable<Category> GetAllCategories();

//     // Obtiene una categoría por su nombre.
//     Category GetCategoryByName(Name name);

//     // Crea una nueva categoría a partir de un DTO (Data Transfer Object).
//     Task<Category> Create(CategoryCreateDTO categoryCreateDTO);

//     // Actualiza una categoría existente identificada por su ID.
//     Task<Category> Update(int id, CategoryUpdateDTO categoryUpdateDTO);

//     // Elimina una categoría de la base de datos por su ID.
//     Task<Category> Delete(int id);

//     // Verifica si una categoría existe en la base de datos por su ID.
//     Task<bool> CheckExistence(int id);
// }
